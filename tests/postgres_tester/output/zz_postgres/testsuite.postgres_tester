# tests with plain sqlite output
simple_offset_plain

# tests with comments
simple_offset_comment

# plain tests but the table already exists
simple_offset_existing

# plain tests where table already exists but dropprevious is set
simple_offset_existing_dropprevious


# check whether using deprecated prefix works
sop_deprecated_prefix

# tests for deprecation warnings when using the old divider
sop_deprecated_divider

# tests for deprecation warnings when using the old divider and a prefix
sop_deprecated_divider_prefix

