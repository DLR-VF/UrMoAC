# tests with plain sqlite output
simple_offset_plain

# tests with comments
simple_offset_comment

# plain tests but the table already exists
simple_offset_existing

# plain tests where table already exists but dropprevious is set
simple_offset_existing_dropprevious


# check whether using deprecated source definitions works
simple_offset_plain_deprecated